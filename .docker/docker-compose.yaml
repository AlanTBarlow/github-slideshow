version: "3.9"

services:
  app:
    build: .
    stdin_open: true
    ports:
      - "4000:4000"
    volumes:
      - type: volume
        source: project-slideshow-dev-environment
        target: /usr/src/app/

volumes:
  project-slideshow-dev-environment:
